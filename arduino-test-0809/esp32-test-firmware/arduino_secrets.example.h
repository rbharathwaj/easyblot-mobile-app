/*
 * TEMPLATE — copy this file to `arduino_secrets.h` in the same folder and put
 * your real Wi-Fi details in the copy. `arduino_secrets.h` is git-ignored, so
 * your credentials never reach the repository.
 *
 * In Arduino IDE:  Sketch -> Add File...  or just copy it in Explorer/Finder.
 *
 * Note: the ESP32 only joins 2.4 GHz networks. If your router publishes one
 * name for both bands, you may need the 2.4 GHz SSID specifically.
 */

#define SECRET_WIFI_SSID     "YOUR_WIFI_NAME"
#define SECRET_WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
